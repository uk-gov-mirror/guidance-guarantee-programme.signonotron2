Signon accounts are suspended after <%= User::SUSPENSION_THRESHOLD_PERIOD.inspect %> of inactivity.

Your Signon <%= account_name %>, for <%= @user.email %>, has now been suspended and you won't be able to access any <%= account_name %> applications.

<% if instance_name.present? %>
<%= account_name.humanize %> suspensions do not suspend production accounts or remove access to production applications.
<% end %>

If you don't need the account you can ignore this email.

If you do still need the account you will have to contact the Signon administrator in your organisation.
