<p>Signon accounts are suspended after <%= User::SUSPENSION_THRESHOLD_PERIOD.inspect %> of inactivity.</p>

<p>Your Signon <%= account_name %>, for <%= @user.email %>, will be suspended <%= suspension_time %>. After suspension you won't be able to access any <%= account_name %> applications.</p>

<% if instance_name.present? %>
<p><%= account_name.humanize %> suspensions do not suspend production accounts or remove access to production applications.</p>
<% end %>

<p>If you don't need the account you can ignore these emails.</p>

<p>To stop your account suspension you will need to <%= link_to 'sign in', new_user_session_url(protocol: 'https') %>.</p>

<p>If you’ve forgotten your passphrase, you can request a new one from the <%= link_to 'sign in', new_user_session_url(protocol: 'https') %> screen.</p>
